module shifter#(parameter WIDTH = 4)(
input clk, 
input arstn, 
input s_in, 
output reg [WIDTH-1:0]out);

always @(posedge clk or negedge arstn) begin
	if(!arstn)
		out <= {WIDTH{1'bo}};
	else
		out <= {1'b0, out[WIDTH-2:0]};
end

endmodule