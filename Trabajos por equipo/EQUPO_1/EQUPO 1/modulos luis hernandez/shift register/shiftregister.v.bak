module shiftregister #(
    parameter DATA_WIDHT = 1,
	 parameter NUM_STAGES = 4
)(
   input clk,
	input reset,
	input [DATA_WIDTH-1:0]
in_data,
   output reg
[DATA_WIDTH-1:0]out_data
);
reg [DATA_WIDTH-1:0]shift_reg
[NUM-STAGES-1:0];

always @(posedge clk or posedge )